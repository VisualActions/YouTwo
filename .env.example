# Copy to .env and fill in. Used by worker/ and rtmp/.
SUPABASE_URL=https://mgmqvpilygkapngythkj.supabase.co
# Dashboard -> Project Settings -> API keys -> service_role (secret!)
SUPABASE_SERVICE_ROLE_KEY=
# Dashboard -> Project Settings -> API keys -> anon (publishable; used by the test scripts)
SUPABASE_ANON_KEY=

# Optional overrides
WORKER_POLL_MS=5000
RTMP_PORT=1935
LIVE_HTTP_PORT=8000

# Only needed if you run worker/seed-demo.mjs
DEMO_PASSWORD=
