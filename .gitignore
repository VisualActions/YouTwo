node_modules/
.next/
.env
.env.local
*.log
tmp/

# design-sync: staged scripts + regenerated build output + machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
.design-sync/upload-batch-*.json

# built kit output (regenerated by `npm run build`)
design-kit/dist/

# rtmp runtime dirs
rtmp/live-media/
rtmp/recordings/

# generated brand art + animation frames (regenerate with render-brand.mjs)
worker/brand/out/

# unrelated nested repo that happens to live in this folder
youtube-clone-html-css/

# never commit secrets
.env
.env.*
!.env.example
*.pem
*.p12
*.mobileprovision
